


pipeline {
    agent any  // This tells Jenkins to use any available agent
    stages {
        stage('Build') {
            steps {
                // Your build steps here
                echo 'Building...'
            }
        }
    }
    post {
        always {
            cleanWs()  // Clean workspace in the post section
        }
    }
}
